diff --git a/src/lib.rs b/src/lib.rs
index 0000000..1111111 100644
--- a/src/lib.rs
+++ b/src/lib.rs
@@ -1,5 +1,7 @@
 pub fn untouched() {}

-pub fn changed(x: i32) -> i32 {
-    x + 1
+pub fn changed(x: i32) -> i32 {
+    if x < 0 { return 0; }
+    x * 2
 }
